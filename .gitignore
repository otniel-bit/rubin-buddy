# Built locally by install.sh / swiftc — never committed, so nobody ends up
# running an unsigned binary they didn't compile.
buddy

.DS_Store
*.tmp
lines.txt.new
# press/make.js intermediates (deleted on success, stranded on failure)
press/*.html
# written beside the script by hooks.sh --remove; state, not source
.hooks-declined
